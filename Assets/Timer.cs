using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Mirror;
public class Timer : NetworkBehaviour
{
    bool should = true;
    // Start is called before the first frame update
    [SerializeField] Text myPing;
    PlayerSyncPosition myPingGetter;
  /*  private void Start()
    {
        if (!isServer)
        {
           
        }
    }*/
  /*  private void Update()
    {
        if (!isServer)
        {
            myPing.text = "" + ;
        }
    }*/
 
  
}
