using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Mirror;

public class Bird : NetworkBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log ("SPAWN");
		//SpawnObj (gameObject, gameObject.transform.position, gameObject.transform.rotation);
	}


}
